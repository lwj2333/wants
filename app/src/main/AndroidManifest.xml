<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lwj.example">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".dialog.DialogActivity" />
        <activity android:name=".tab.TabActivity" >

        </activity>
        <activity android:name=".recyclerview.RecyclerActivity" />
        <activity android:name=".recyclerview.SimpleActivity" />
        <activity android:name=".recyclerview.MoreActivity" />
        <activity android:name=".spinner.SpinnerActivity" >

        </activity>
        <activity android:name=".recyclerview.SlidingActivity" >

        </activity>
        <activity android:name=".recyclerview.EventTestActivity" >

        </activity>
        <activity android:name=".recyclerview.widgets.AnimationSlidingActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>